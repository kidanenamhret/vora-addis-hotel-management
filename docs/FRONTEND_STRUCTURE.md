# Frontend Folder Structure

```text
frontend/
  src/
    components/      Shared layout and UI building blocks
    data/            Website content and static display data
    pages/           Website pages, login, booking, dashboards
    services/        Axios API client and auth helpers
```

The first screen is the usable hotel website, not a marketing placeholder. Staff can access `/dashboard` and `/login`.
